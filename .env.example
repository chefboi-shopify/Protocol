# Database (SQLite for dev, works out of the box)
DATABASE_URL="file:./dev.db"

# Optional: Stripe (leave empty if not using payments yet)
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
EXTENSION_FEE_CENTS=300
SEASON_PASS_CENTS=9000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: Pusher (for real-time chat)
NEXT_PUBLIC_PUSHER_KEY=
NEXT_PUBLIC_PUSHER_CLUSTER=
PUSHER_APP_ID=
PUSHER_SECRET=

# Optional: Video storage
BLOB_READ_WRITE_TOKEN=
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=

# Optional: LLM screening
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: Cron auth
CRON_SECRET=
